# AGENTS.md — 여기 DC 돼요? 후보 부지 1차 사업검토

2026-09-08 구현·운영 확인 기준. 실행 절차는 [README.md](README.md), 산식·범위는 [docs/PLAN.md](docs/PLAN.md), 자료 출처·한계는 [docs/DATA.md](docs/DATA.md), 데모·검증 기록은 [docs/DEMO.md](docs/DEMO.md)를 따른다.

## 프로젝트와 현재 상태

- 제안받은 후보 주소와 사업조건을 입력해 주요 제약, 면적 부족, 미확인 비용·공급조건, 다음 확인사항을 정리하는 지도 웹앱이다. 주 사용자는 개발·사업검토 담당자이며 초기 검토와 내부 보고를 돕는다.
- 참고점수는 보조 정보다. 점수로 사업 적합·부적합, 실제 전력 공급량이나 인허가 결과를 확정하지 않는다. 기본 보고서는 AI 없이 출력되고 AI 의견은 선택 기능이다.
- 화면 서비스명은 **여기 DC 돼요? (가칭)**. Vercel 프로젝트명 `grand-site-dc`와 `X-Title`의 내부 코드명은 유지한다. PLAI CAMP S3 개발자리그(9.21~22) 해커톤 프로토타입이다.
- 운영 주소: https://grand-site-dc.vercel.app. 2026-09-08 배포 코드 `7678816`의 Ready 상태와 실제 서버 응답을 확인했다. 최신 배포 여부를 말할 때는 이 기록만 재사용하지 말고 현재 배포와 대조한다.
- 1차 구현 완료: 전국 가구 격자, 공통 사업조건·부지별 조건, 면적·비용·금융비용 계산, 협의 기록, 최대 4개 후보 비교, 첫 장 요약·18개 근거 항목의 보고서, AI 의견 유효성 검사.
- 해당 변경 검증: 타입 검사·린트·17개 파일 134개 테스트·데이터 검증·프로덕션 빌드 통과. 모바일·데스크톱·A4와 운영 AI 응답 검증의 상세 조건은 DEMO에 있다. 이후 코드 변경의 검증을 이 기록으로 대신하지 않는다.

## 세션 시작

1. 사용자 요청 범위, `git status --short`, 현재 브랜치와 기존 변경을 확인한다. 사용자 변경·미추적 파일을 임의로 지우거나 덮어쓰지 않는다.
2. README의 현재 상태·다음 단계와 필요한 키 표를 확인한다. 키가 실제 작업에 필요하고 등록되지 않았다면 발급 URL·절차를 안내한다. 가입·키 발급은 대행하지 않으며 비밀값은 사용자가 직접 입력한다. 키가 이미 등록돼 있으면 재입력을 요구하지 않는다.
3. 평소에는 커밋된 `prototype/public/data`를 사용한다. 데이터 갱신이 필요한 경우에만 해당 전처리를 실행한다. 전체 수집을 매 세션 반복하지 않는다.

## 구현 규칙

- 대화·문서·커밋 메시지는 한국어, 코드 식별자·주석은 영어. TypeScript strict, 들여쓰기 2칸, 함수형 컴포넌트와 Hooks를 사용한다. 새 상태관리·라우터·차트 라이브러리는 추가하지 않는다.
- 점수·면적·사업비·금융비용·주요 제약·추가 확인사항의 판단과 계산은 `prototype/src/scoring/engine.ts`의 `scoreSite()`에 둔다. UI는 결과를 표시한다.
- 가중치·임계값·통계·단위 변환 계수·금리와 기간 가정은 `data-pack/curated/constants.json`에서 관리한다. 앱 복사본만 직접 고치지 말고 `build_all.py --sync-only`로 검증·동기화한다.
- 엔진 변경 시 의도한 요구사항에 맞춰 `prototype/src/scoring/engine.test.ts` 골든 기대값을 먼저 갱신하고 통과시킨다. 실패를 숨기기 위해 현재 출력에 기대값을 맞추지 않는다. 규제·재해·자료 범위·비교·보고서 회귀도 확인한다.
- `ScoreInput.project`의 `ProjectAssumptions`는 공통 사업·설계·금리 조건, `ScoreInput.conditions`의 `SiteConditions`는 부지별 면적·비용·협의 기록이다. 기존 `standard/small/hyperscale` 식별자는 각각 일반 클라우드/엣지/AIDC이며 고정 MW를 뜻하지 않는다.
- 비교는 `prototype/src/compare/pins.ts`에서 최대 4곳을 보관한다. 입력·자료가 바뀌면 `scoreSite()`로 다시 계산하고, 비용 차액은 엔진 결과끼리의 뺄셈만 한다. 모든 후보의 비용이 완전하고 방식·범위가 같을 때만 차액을 표시한다. 후보는 현재 세션 동안만 보관된다.
- 입력·근거 변경 시 실행 중이거나 완료된 AI 의견과 사전 생성 의견의 유효성을 해제한다. `prototype/src/genai/memoContext.ts`의 v4 서명은 전체 입력·결과·체크리스트를 포함한다. 예전 의견을 새 결과에 붙이거나 조건을 되돌렸다는 이유만으로 중단된 의견을 되살리지 않는다.

## 계산과 해석에서 지킬 사항

### 사업조건·면적·비용

- 초기 사업 유형은 일반 클라우드, 신축·계획 연면적 직접 입력 방식이다. 목표 수전용량과 IT부하는 별도 입력이며 유형 변경으로 용량·비용을 덮어쓰지 않는다. 실제 면적·설계계수·인입비·차입잔액에 임의 기본값을 넣지 않는다.
- 상세 설계는 `랙 수 = ceil(IT부하 MW × 1,000 / 랙당 전력 kW)`, `필요 연면적 = 랙 수 × 통로 포함 랙당 면적 / (전산실 비중 % / 100)`이다. 수전용량으로 IT부하를 대체하지 않는다.
- 신축 최소 대지는 `max(필요 연면적 / (용적률 % / 100), 필요 연면적 / 지상층수 / (건폐율 % / 100))`이다. 모든 면적이 지상·용적률 산입이고 층별 면적이 같다는 단순 가정이다. 옥외설비·주차·높이·이격거리 검토는 남긴다.
- 기존 건물 전환은 확보 건물 면적과 필요 연면적을 비교하고 구조하중·층고·장비 반입·냉각설비 설치를 추가 확인사항으로 제시한다.
- 미입력은 `null`이다. 필수 양수의 누락·0·음수·비유한값, 소수 층수, 100% 초과 건폐율/전산실 비중은 계산 보류다. 비용과 차입잔액의 명시적 0은 허용하되 결측을 0으로 바꾸지 않는다.
- 사업비는 항목 합계 또는 총액 직접 입력 중 선택한 한 방식만 사용한다. 누락 비용이 있으면 “입력된 비용 합계”와 누락 항목을 표시하고 최저비용으로 단정하지 않는다.
- 금융비용은 `지연 중 평균 차입잔액 × 연 금리 × 지연개월 / 12`다. 총사업비를 차입잔액으로 대신 사용하거나 점수에서 지연기간을 예측하지 않는다. 금리 4.5/5.5/6.5%, 기간 6/12/24개월은 수정 가능한 계산 가정이다.
- 전력·용수·통신은 미확인/협의 중/사용자 확인, 내용·날짜를 기록한다. 사용자 확인은 공급기관 확약을 서비스가 검증했다는 뜻이 아니다. 용수 시설용량으로 개별 공급 가능량을 계산하지 않는다.

### 참고점수·주변 현황

- 전력 공개 목록과 OSM 위치를 구분한다. 변전소 개수로 공급 MW를 만들지 않고 직선거리를 선로 공사거리로 해석하지 않는다. 실제 연결 변전소·전력계통영향평가 결과는 미확인이다. 발전접속 여유용량은 수전 판단에 직접 쓰지 않는다.
- 유형별 임의 감점배수·변전소 개수 합격조건·미등재 D등급 상한은 폐기했다. 기본 전력/인허가 가중치는 유지한다. 전력 목록 미등재·조회 실패만으로 부지를 제외하지 않는다.
- 필요한 자료가 없으면 참고점수는 미산정이다. 전력은 행정구역·읍면동 공개 목록의 변전소 등재·최근접 위치, 인허가는 행정구역·용도지역·인구·학교·지형·번들 규제·VWorld 규제/재해 상태를 확인한다. 정확한 조건은 엔진을 따른다.
- 확인된 법적 입지 제한은 40점 감점과 E등급 상한·붉은 배지를 유지한다. 다른 자료가 미확인이어도 확인된 제한을 해제하지 않는다. 규제 검토는 15점, 재해위험지구는 15점 1회와 추가 확인을 적용한다. 재해만으로 E 상한을 만들거나 같은 재해를 규제구역으로 중복 감점하지 않는다.
- 인구·가구는 반경 1km 내 격자 중심점 합계다. 가구 수는 주민등록 세대수와 구분하고 인구와 중복 감점하지 않는다. 부분 합계·결측은 표시한다. 인구·가구가 적다는 이유로 수용성이 좋다고 판단하지 않는다.
- 학교 지점까지 직선거리는 법정 교육환경보호구역 경계거리가 아니다. 뉴스·사례는 참고 목록이며 감점·수용성 등급에서 제외한다. 기사 0건과 미수집 지역을 구분한다.
- 용도지역 명칭만으로 법적 입지 제한을 확정하거나, 재해 미해당·육지 판독을 안전/규제 없음으로 확대 해석하지 않는다. 해상·자료 범위 밖·조회 실패의 기존 보류 처리를 유지한다.

## 데이터와 전처리

- 인구 `pop_grid.json`: SGIS 2024 전국 1km, 유효 인구 73,016셀. 옛 21,944셀 한정본은 사용하지 않는다.
- 가구 `households_grid.json`: 같은 SGIS ZIP의 `to_ga_001`, 경계 107,680셀 중 값 확인 74,723개·결측 32,957개. 유효값 합계 22,321,130은 비밀보호 조정이 포함된 참고 통계다. 원본 SHA-256·출처·기준연도·공간 단위·가공 버전 p09-v1을 저장한다. 새 키는 필요 없다.
- SGIS 갱신: `fetch_raw.py sgis` → `p03_population.py` → `p09_households.py` → `build_all.py --sync-only`. 중복 코드·좌표·결측·전국 범위를 검증한다.
- 보호지역은 국립공원 23곳(2024-12-30)과 KDPA(2016-12-31) 기반 번들, VWorld 실시간 규제 조회를 별도로 추적한다. 번들은 통합 1,516개 도형이며 군사시설보호구역은 미반영이다. 최신 고시 도면을 대체하지 않는다.
- 보호지역 갱신: `fetch_raw.py protected` → `p08_protected_zones.py --probe` → `p08_protected_zones.py` → `build_all.py --sync-only`. VWorld 레이어별 범위·이용조건은 DATA와 `constants.scoring.restriction`을 따른다.
- 지형은 SRTM 30m·2000년 촬영과 ETOPO1 기반 0.01° 격자다. 이후 매립한 송도·새만금·시화 등은 `reclaimedOverrides`와 VWorld 용도지역으로 보완한다. 내륙 수면·연약지반은 별도 확인한다.
- 자료 범위는 `constants.scoring.coverage`의 bbox·휴전선/NLL 근사선·최근접 읍면동 중심점 거리로 정한다. `site.status='outside'`는 “판독 불가 · 자료 범위 밖”이다. 북한·국외도 “자료 범위 밖”으로 쓴다. 해상·수역은 참고점수·비교·보고서 대상에서 제외한다.
- 건축HUB 허가→착공 통계와 뉴스는 빌드 시 수집한다. 뉴스는 NAVER API HUB 주소·헤더를 사용한다. 사례 이름·과거 시장 수치를 인용할 때는 DATA와 개별 출처·기간을 확인한다. 안양 사례는 호계동이며 관양동이 아니다. 736건 기술검토 통계 기간은 2024-08-01~2026-03-27로 현재 시장 확률이 아니다.
- p03·p09·p08에는 `pyshp`, `pyproj`가 필요하다. Python 3.9에서는 별도 가상환경에 `pyshp`와 `pyproj<3.7`을 설치한다. 공공 CSV는 cp949 우선, 실패 시 utf-8-sig이며 상가업소 CSV는 UTF-8이다.

## 온라인 API·키·배포

- 런타임 외부 API 의존을 늘리지 않는다. 기존 온라인 기능은 지도 타일, OpenRouter, VWorld `geocode.ts`(주소), `zoning.ts`(용도지역), `restrictions.ts`(규제), `disaster.ts`(재해), `wms.ts`(지도 표시)다. 새 자료는 원본·이용조건·가공을 검증한 뒤 빌드 시 JSON으로 묶는다.
- VWorld API는 `runtime: 'edge', regions: ['icn1']`을 유지한다. 서버 함수의 상대 import는 `.js`를 사용한다. Edge에서 Node 파일 API에 의존하는 공급자 SDK를 추가하지 않는다. AI는 `fetch`와 SSE를 사용한다.
- 런타임 키는 Vercel 서버 환경변수에만 두고 데이터 수집 키는 GitHub Actions Secrets에서 관리한다. Codex Cloud 환경에는 API 비밀값을 복제하지 않는다. 대화·명령 인수·로그·코드·브라우저 입력 UI·번들·커밋에 비밀값을 넣지 않는다. 값 대신 등록 여부와 응답으로 확인한다.
- 2026-09-08 운영 `OPENROUTER_API_KEY`의 Production Secret 등록과 AI 응답을 확인했다. 등록·교체는 README의 Vercel 설정 절차를 따른다. `LLM_MODEL`은 비밀키가 아닌 모델 설정값이다. 환경변수 변경 후 새 배포가 필요하다.
- 서버 기본 `LLM_MODEL`은 `google/gemma-4-31b-it:free`. 기본값일 때 `nvidia/nemotron-3.5-lightning:free`, `google/gemma-4-26b-a4b-it:free`를 대체 목록으로 전송한다. 자동 유료 전환은 없다. 다른 모델을 명시하면 해당 모델만 사용한다. 모델 제공 상태는 변경 시 확인한다.
- AI는 추론 모드 비활성·출력 4,000토큰이며 빈 응답/실패는 사용자에게 표시한다. SSE `[DONE]`에서 응답을 종료한다. 대체 목록 사용 시 실제 모델을 단정하지 않고 OpenRouter로 표시한다. 실패해도 기본 보고서는 출력된다.
- 사전 생성 스크립트는 `OPENROUTER_API_KEY`와 선택 `OPENROUTER_MODEL`을 사용한다. 서버 변수 `LLM_MODEL`과 혼동하지 않는다. 현재 `precomputed_memos.json`은 없으며 예전 Gemini 키나 시나리오 id→텍스트 형식은 사용하지 않는다. 생성본은 v4와 현재 전체 평가 서명이 맞아야 한다.
- 로컬 Vite `/api/*`는 운영 서버로 전달된다. 로컬에서 `prototype/api`만 수정해도 운영 함수가 바뀌지는 않는다. 서버 수정은 API 테스트와 배포 후 응답으로 확인한다.
- 자동 배포: `.github/workflows/vercel-prod.yml`, 기본 브랜치 `prototype`의 `prototype/**` 또는 워크플로 변경 푸시, 그리고 수동 `workflow_dispatch`. Vercel Git 연동의 Root Directory는 `prototype`이다. GitHub Actions Secret `VERCEL_TOKEN`으로 Vercel CLI 59.16.0을 실행하며 품질검사, 명시적 프로젝트 연결, production pull/build, prebuilt 배포, Ready 확인 순서다. README·AGENTS만 바뀐 푸시는 자동 배포하지 않는다.
- 원격은 `git@github.com:seungminyi-byte/Camp_It_Ralph.git`, 단일 기준 브랜치는 `prototype`이다. 변경은 작업 브랜치와 PR로 반영한다. 기존 `seungminyi-byte-prototype` 원격 브랜치는 과거 기록으로 유지하되 작업 기준으로 사용하지 않는다. **푸시·배포는 사용자가 요청할 때만** 진행한다. 배포 완료는 원격 반영, Vercel 상태, 운영 정적 파일·API 응답을 구분해 확인한다.

## Codex Cloud

- 개인 환경 이름은 `camp-itralph`, 환경 저장소는 `seungminyi-byte/Camp_It_Ralph`, 기준 브랜치는 `prototype`이다. Codex GitHub 앱은 사용자 요청에 따라 `seungminyi-byte` 계정 전체 저장소 접근 권한을 유지하며 특정 저장소로 제한하지 않는다. 구성·실행 완료 여부는 DEMO의 검증 기록을 따른다.
- Node.js 24와 Python 3.12를 사용한다. 설정 스크립트는 `npm --prefix prototype ci`와 `python3 -m pip install pyshp 'pyproj<3.7'`, 유지관리 스크립트는 `npm --prefix prototype ci`다.
- 환경에 API 비밀값을 넣지 않는다. 에이전트 인터넷 접근은 `grand-site-dc.vercel.app`의 `GET`, `HEAD`, `OPTIONS`, `POST`만 허용한다.
- GitHub의 수동 `@codex` 작업과 리뷰를 사용할 수 있게 하되 자동 리뷰는 사용하지 않는다.
- 검사 전용 작업은 타입 검사, 린트, 전체 Vitest, 데이터 검증, 프로덕션 빌드를 실행하고 `git diff --exit-code`와 `git status --short`가 깨끗한지 확인한다. 결과에는 이 파일을 읽고 적용했음을 명시한다.

## 표현 규칙

- 화면·데이터·프롬프트·문서에 회사명(사업자명), “자사”, 특정 사업장 고유명을 단가·규모 예시로 쓰지 않는다. 메모 페르소나는 가상의 “데이터센터팀”이고 사례는 지역명·시설 유형으로 서술한다.
- 출처 URL·언론사명·데이터 출처 기관·서비스명은 유지한다. 사용자 확정 예외: 링크가 있는 `news_signal.json` 자동 기사 제목은 원문을 유지하며 제목 속 회사명·실명을 임의로 고치지 않는다.
- “스크리닝 참고용, 한전 공식 검토·법률 판단 대체 불가” 문구를 유지한다.
- 화면·보고서·AI 프롬프트에서 영어 직역체(축·게이트·앵커·시그널·매칭·센트로이드·점추정·로드·프록시·오버레이)를 피한다. “참고점수”, “주변 인구·가구”, “참고 기사”, “행정구역 미확인”, “가장 가까운 읍면동 중심점”, “자료 범위 밖”으로 설명한다.
- 법정 제한은 “법적 입지 제한 구역”, “규제구역 검토 필요”, “법정 보호·규제구역 해당으로 E등급으로 제한”을 쓴다. 감점 라벨 변경 시 `prototype/src/report/checklist.ts`와 관련 테스트의 문자열 키도 함께 확인한다.

## 실행·검증 명령

저장소 루트에서 실행한다. Node.js 24는 배포 자동화 기준이다.

```bash
npm --prefix prototype install
npm --prefix prototype run dev -- --port 5199
```

별도 터미널에서 검증한다. `typecheck`는 앱·API·사전 생성 스크립트를 모두 검사한다.

```bash
npm --prefix prototype run typecheck
npm --prefix prototype run lint
(cd prototype && node node_modules/vitest/vitest.mjs run)
python3 data-pack/scripts/validate_out.py
npm --prefix prototype run build
```

데이터만 동기화할 때는 `python3 data-pack/scripts/build_all.py --sync-only`를 쓴다. 전체 `build_all.py`는 원본 전처리와 허가·뉴스 등 외부 수집도 실행하므로 갱신 범위에 맞춰 선택한다. 문서만 바꾼 경우에는 링크·명령·수치·현재 코드와의 일치 및 diff를 확인하며 불필요하게 데이터를 재수집하지 않는다.

필수 회귀 예시:

- 연면적 30,000㎡·용적률 200%·건폐율 50%·4층 → 최소 대지 15,000㎡. 대지 10,000㎡이면 5,000㎡ 부족. 10,000㎡ ↔ 3,025평 변환 확인.
- 평균 차입잔액 1,000억원·연 6%·12개월 → 60억원. 미입력 차입잔액은 계산 보류, 누락 인입비는 최저비용 근거로 사용하지 않음.
- 고정 테스트의 규제·재해 정상 미해당 조건: 고양 덕이동 참고점수 미산정(용도지역 미확인), 인천 청천동 E32, 세종 반곡동 B77. 반경 1km 가구 수는 각각 9,091/15,809/4,496. 실시간 조회 결과를 이 값으로 덮어쓰지 않는다.
- 과거 D45/E32/B75·주민 갈등 등급·688/688/103억원은 폐기한 기준이다. 세종 지형 경사 5° 부근의 감점 경계, 보호구역 E 상한, 재해 15점 1회, 해상·자료 범위 밖·조회 실패를 계속 감시한다.
- 입력·근거 변경 후 비교 재계산과 AI 의견 해제, AI 없이 기본 보고서, 모바일·데스크톱·A4 출력은 DEMO 절차로 확인한다.

## 남은 작업

1. 기후: 1991~2020 평년값 파일셋은 요청 지연으로 원본 확보·이용조건·가공 검증 미완료다. 월별 기온·습도·관측소 위치를 검증한 뒤 번들로 추가한다. 냉각 전력비·용수량으로 자동 환산하지 않는다.
2. IC: 위치정보 서비스 활용신청·인증·실제 응답 미검증이다. 필요한 신청은 사용자에게 안내하고 IC만 추출해 좌표·중복·이용조건을 확인한다. 두 자료 모두 현재 화면에 없으며 완료 조건은 PLAN·DATA를 따른다.
3. 해커톤 준비: v4 사전 생성 의견 생성·일치 조건 확인, 현장 재빌드 팩 `ralphathon/`(현재 미생성), 빈 폴더 재빌드 드라이런. DEMO 문서는 이미 작성되어 있다.

전국 필지·건축물대장·토지 매입가 자동 추정·통신/송전선로 상세 경로·철도역 수집은 이번 구현 범위에서 제외했다.
